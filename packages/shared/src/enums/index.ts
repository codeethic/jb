export enum MealPeriod {
  LUNCH = 'lunch',
  DINNER = 'dinner',
}

export enum FeatureStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  EIGHTY_SIXED = '86d',
}

export enum UserRole {
  SERVER = 'server',
  CHEF = 'chef',
  MANAGER = 'manager',
  ADMIN = 'admin',
}
